<ul class="replies">
    <% for(var i=0; i<replies.length; i++) {%>
        <li class="reply">
            <span><%= replies[i].name %></span>
            <span><%= replies[i].quote %></span>
        </li>
        <% } %>
</ul>
